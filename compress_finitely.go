package main

import (
	"fmt"
)

func main() {
	precision := 32
	whole := math.Pos(2, precision)
	half := whole / 2
	quarter := whole / 4

	X := []byte{'A', 'B'}
	r := []byte{2, 2}
	R := 0
	for i := 0; i < len(r); i++ {
		R += r[i]
	}
	p = []float64{}
	for i := 0; i < len(r); i++ {
		p = append(p, float64(r[i])/R)
	}
	EOF := 0
	x = "BABAB"

	c = []byte{0}
	for i := 1; i < len(p); i++ {
		sum := 0
		for j := 1
	}

	fmt.Println("Hello Universe!")
}

